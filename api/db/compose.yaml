services:
  postgres:
    container_name: postgres
    image: postgres:latest
    environment:
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=linoDB
    ports:
      - "5432:5432"
    restart: always
